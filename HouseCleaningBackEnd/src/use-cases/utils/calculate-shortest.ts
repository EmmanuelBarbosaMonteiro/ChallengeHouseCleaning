
export function getDistanceBetweenPoints(xCoordinate: number, yCoordinate: number) {
    const differenceInX = xCoordinate - 0;
    const differenceInY  = yCoordinate - 0;
    return Math.sqrt(differenceInX * differenceInX + differenceInY * differenceInY);
}